# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - m2mclothingg

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://m2mclothingimg.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_b1c80f2efcf24d039fe29edca48008f5 }}
        password: ${{ secrets.AzureAppService_ContainerPassword_3e36fa89de3942a6b9822cba53dca8e7 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: m2mclothingimg.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_b1c80f2efcf24d039fe29edca48008f5 }}/m2mclothingimg:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'm2mclothingg'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_779257442b244f53b884b3c05ee25cdb }}
        images: 'm2mclothingimg.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_b1c80f2efcf24d039fe29edca48008f5 }}/m2mclothingimg:${{ github.sha }}'